# Broos Console

The next-generation Broos Cloud interface. This application is intentionally isolated from Authentic Theme's compiled assets so upstream theme updates remain manageable.

```bash
npm install
npm run dev
npm run build
```

The current dashboard uses representative fixture data. It does not perform server mutations or collect credentials. Connect it to the versioned Broos Core API after the session and read-only contracts are implemented.
